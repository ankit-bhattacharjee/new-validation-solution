package com.wipro.holmes.uhg.enb.nvs.models;

import java.lang.Number;

rule "relationship-code"
	dialect "mvel"
	when
		field : Field( name == "Relationship Code" && ( value.length() != ( size ) || value.startsWith("A")))
	then
		field.setError( Optional.of("invalid relationship code!") );
end
