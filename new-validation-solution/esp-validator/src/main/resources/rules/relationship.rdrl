package com.wipro.holmes.uhg.enb.esp.models;

import java.lang.Number;
import java.util.Optional;

rule "relationship"
	dialect "mvel"
	when
		field : Field( name == "Relationship" && value.trim().length() > 0 )
	then
		field.setError( "Relationship Cannot Be Blank!");
end
